<h1 class="mt-5"><%= t('edit vacation') %></h1>

<%= render "form", vacation: @vacation %>

<br>

<div>
  <%= link_to t("show this vacation"), @vacation %> |
  <%= link_to t("back to vacations"), vacations_path %>
</div>
