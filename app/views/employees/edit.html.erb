<h1 class="mt-5"><%= t('editing employee') %></h1>

<%= render "form", employee: @employee %>

<br>

<div>
  <%= link_to t("show this employee"), @employee %> |
  <%= link_to t("back to employees"), employees_path %>
</div>
